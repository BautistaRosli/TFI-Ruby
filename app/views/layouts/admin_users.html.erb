<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Usuarios - Disquería</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
    <%= stylesheet_link_tag 'users_index', media: 'all' %>
    <%= yield :stylesheets %>
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <header class="users-header w-100">
        <div class="header-content">
            <div class="header-top">
                <div>
                    <h1 class="header-title">Gestión de Usuarios</h1>
                    <p class="header-subtitle">Disquería - Sistema de Gestión</p>
                    <%= link_to "Volver a menú de gestión", admin_dashboard_path, class: "btn btn-logout" %>
                </div>
                
                <div class="dropdown">
                    <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                        Mi Perfil
                    </button>

                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <%= link_to "Ver perfil", admin_user_path(current_user), class: "dropdown-item" %>
                        </li>

                        <li>
                            <%= link_to "Salir", destroy_user_session_path, method: :get, class: "dropdown-item text-danger" %>
                        </li>
                    </ul>
                </div>


            </div>
        </div>
    </header>

    <div class="flash-messages">
        <% if flash.any? %>
            <div class="flash-notifications">
            <% flash.each do |type, message| %>         
                <% 
                alert_class = case type.to_s
                when 'notice' then 'alert-success'
                when 'alert' then 'alert-danger'
                when 'warning' then 'alert-warning'
                when 'info' then 'alert-info'
                else 'alert-primary'
                end
                %>
                <div class="alert <%= alert_class %> alert-dismissible fade show" role="alert">
                    <span class="alert-message"><%= message %></span>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% end %>
            </div>
        <% end %>
    </div>

    <%= yield %>

    

</body>
</html>


